package com.backend.bandtito.utils;

import com.backend.bandtito.models.Band;
import com.backend.bandtito.models.Musician;
import com.backend.bandtito.repositories.BandRepository;

import org.springframework.beans.factory.annotation.Autowired;

public class BandManagement{
    
    //@Autowired
    //private BandRepository BandRepo;

    /*public void createBand(String name, String address, Musician admin){
        //Band band = new Band(name, address, admin);
        //BandRepo.save(band);

        BandRepository repo = context
        .getBean(BandRepository.class);

        Band pants = new Band(
        name, address, admin);
        pants = repo.save(pants);

    }*/

}